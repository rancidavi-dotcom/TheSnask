class User
    let name = "";
    fun init(n)
        self.name = n;

class main
    fun start()
        mut persistent_ref = nil;
        zone "request_scope"
            let ephemeral_obj = new User("davidev");
            promote ephemeral_obj to heap;
            persistent_ref = ephemeral_obj;
        print(persistent_ref.name);
