# GUI Shaders (Snask Library)

`gui_shaders` is an add-on library for Snask GUI projects focused on styling via modern CSS.

## Install
```bash
snask install gui_shaders
```

## Usage
```snask
import "gui";
import "gui_shaders";
```

