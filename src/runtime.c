// Runtime modular:
// - Este arquivo é a “entrada” compilada pelo `snask setup`.
// - O corpo antigo (~3k linhas) foi movido para `src/runtime/runtime_old.c`.
// - Próximo passo: ir extraindo por módulos (gui.c, json.c, sfs.c, etc.) mantendo compat.

#include "runtime/runtime_old.c"
