import "prelude";
import "string";
import "json";
import "os";

class main
    fun start()
        println("==============================================");
        println("   SNASK MODULAR RUNTIME - STRESS TEST       ");
        println("==============================================");

        // 1. Teste de String
        let s = "  snask primordial  ";
        let trimmed = string::trim(s);
        let up = string::upper(trimmed);
        let l = string::len(up);
        let l_str = num_to_str(l);
        println("String Test: [" + up + "] (Len: " + l_str + ")");
        
        // 2. Teste de OS
        let plat = os::platform();
        let current = os::cwd();
        println("OS Platform: " + plat);
        println("Current Dir: " + current);

        // 3. Teste de JSON
        let obj = json::parse("{\"name\":\"Snask\",\"status\":\"modular\"}");
        let name = json::get(obj, "name");
        let status = json::get(obj, "status");
        println("JSON Test: Name=" + name + ", Status=" + status);
        
        // 4. Teste de Math
        let v1 = abs(-100);
        let v2 = sqrt(64);
        let val = v1 + v2;
        let val_s = num_to_str(val);
        println("Math Test: abs(-100) + sqrt(64) = " + val_s);

        println("==============================================");
        println("   RESULTADO: RUNTIME ESTÁ TININDO! 🚀       ");
        println("==============================================");
